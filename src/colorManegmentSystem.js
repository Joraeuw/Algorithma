import store from './redux/store';
const { nodes } = store.getState().panelState;
const { currentFrame } = store.getState().mediaControls;

//TODO: Create color scheme for default and non-default classes


//TODO: For each node/command/curve that is selected in a given triplet one must 
  //TODO: change color accordingly 
function reevaluate() {

}
