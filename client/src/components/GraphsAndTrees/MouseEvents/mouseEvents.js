import handleMouseUpOnNode from './handleMouseUpOnNode';
import handleMouseDown from './handleMouseDown';
import handleMouseLeave from './handleMouseLeave';
import handleMouseUp from './handleMouseUp';
import handleMouseDownOnNode from './handleMouseDownOnNode';
import handleMouseMove from './handleMouseMove';

const mouseEvents = {
  handleMouseUpOnNode,
  handleMouseDown,
  handleMouseLeave,
  handleMouseUp,
  handleMouseDownOnNode,
  handleMouseMove,
};
export default mouseEvents;
